# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# SUPABASE: Required for authentication, player IDs, and cloud database persistence.
# Obtain these from your Supabase Project Settings > API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# VITE_SERVER_URL: Optional external backend server URL (e.g., https://your-backend.onrender.com)
# Only needed if deploying the frontend to a static host like Cloudflare Pages while hosting the Node backend elsewhere.
# Leave blank when deploying as a full-stack container (e.g., Cloud Run, Render, Railway, VPS).
VITE_SERVER_URL=

